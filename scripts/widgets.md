# Test Gear — instruments with no rack space

**Modules** chart = DreamerMPX/mpxChart, arp = HamptonHarmonics/Arp, audio = Core/AudioInterface2, clarity = DreamerDevelopment/Clarity, gear = DreamerDevelopment/TestGear
**Voice** Karen (Premium)
**Rate** 193
**Badges** off
**Captions** off
**Master** audio:Level
**Duck** 10 dB
**Before** clarity:Add and move = 0
**Pacing** perform 0.7, arrive 0.5, beat 0.4, settle 0.5, hold 0.5

## zoom gear 1.3
This is Test Gear. It holds no controls of its own worth speaking of, and that is the point: everything it offers is clipped onto a port, so a rack full of instruments costs no rack space at all.

## zoom chart 1.2
Right-clicking any port offers them.

## menu chart:"Chord tones" "Widgets…" "Voltmeter"
A voltmeter, for instance.

## press chart@50%,72%
It follows the pointer until a click puts it down.

## wait 3
There is the voltage on that jack, changing as the chart plays.

## press widget
Clicking it turns it over: now it reads the peak of the last quarter second, held long enough to read. That is found at the engine's own rate, not once a frame, so it catches what the eye cannot.

## wait 4

## menu chart:"Chord tones" "Widgets…" "Switch"
And a switch.

## press chart@50%,84%

## wait 2
Its light is on, so the signal is getting through.

## press widget
Off. The cable is gone from the port, and the stub left behind says where it went and what colour it was — put the switch back on and it returns exactly as it was.

## wait 4

## press widget
Nothing was altered, so there is nothing to put right.

## wait 3

## say
Neither of these takes anything out of the signal, and neither takes any rack space. There is an oscilloscope, a spectrum analyser and an audio monitor on the same menu, and a set of generators that work on a port that already has a cable in it.
